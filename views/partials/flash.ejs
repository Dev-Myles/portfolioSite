<% if(success && success.length) {%>
    <div class="alert-success" id="flash-message" role="alert">
       <div><%= success %> <div id="x-out-mess">X</div></div> 
     
    </div>
    <% } %>
    
    <% if(error && error.length) {%>
    <div class="alert-failure" id="flash-message" role="alert">
        <div><%= error %><div id="x-out-mess">X</div></div>
        
        </button>
    </div>
    <% } %>